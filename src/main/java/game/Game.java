package game;

import javax.swing.*;
import java.awt.*;

public class Game extends JPanel {
    public Game() {
        setPreferredSize(new Dimension(400, 400));
        setBackground(Color.YELLOW);
    }

    // Add game logic and rendering methods here
}